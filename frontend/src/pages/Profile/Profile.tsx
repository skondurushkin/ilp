import { TypedLink } from '../../router';

export const Profile = () => {
    return (
        <div>
            <h1>Profile</h1>
            <TypedLink to="/">Go to main page</TypedLink>
        </div>
    );
};
