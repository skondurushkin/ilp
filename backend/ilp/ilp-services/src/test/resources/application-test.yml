spring:
  datasource:
    url: jdbc:tc:postgresql:15.0://localhost:5437/postgres
    username: postgres
    password: postgres
  jpa:
    show-sql: true
  flyway:
    enabled: true

jwt:
  tokenDuration: PT10M
  refreshDuration: PT1H

testcontainers:
  reuse:
    enable: true
